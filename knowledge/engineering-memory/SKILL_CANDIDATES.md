# Skill Improvement Candidates

Add a sanitized entry when a pattern repeats three times or a serious incident exposes a missing safeguard. Do not edit core rules during an unrelated incident.

| Date | Pattern | Evidence | Proposed improvement | Expected benefit | Approval |
| --- | --- | --- | --- | --- | --- |
| 2026-08-24 | `@AuraEnabled` methods taking a wrapper object/list parameter directly silently null out on Aura-hosted (classic App Builder) LWC pages | Multi-hour live-org investigation on the Activities List build; see `cases/aura-hosted-lwc-auraenabled-object-param-nulled.md` | Update `apex`/`lwc` skills: (1) default new `@AuraEnabled` methods with non-primitive parameters to the JSON-string + `JSON.deserialize` pattern rather than a typed wrapper parameter; (2) require at least one real-browser end-to-end check (not just anonymous Apex / unit tests) before marking a new Apex-backed LWC feature done | Prevents a class of bug that unit tests and direct Apex calls cannot catch at all, and that silently returns wrong data with no error | Pending |
| 2026-08-26 | Custom-type `lightning-datatable` work repeatedly broke on undocumented-by-default behaviour: a column editing one field while displaying another never refreshes its cell; handlers in a custom `editTemplate` are not bound to the extending class; internal scroll (and with it a reachable inline-edit Save/Cancel bar) needs a definite height on the datatable element; row numbers restart per page without `row-number-offset` | Activities List build - three rounds of failed fixes before the mechanism was identified; see `cases/datatable-custom-type-edited-field-vs-displayed-field.md` and `cases/datatable-fixed-height-scroll-and-paged-row-numbers.md` | Add a datatable checklist to the `lwc` skill for editable/paged datatables: edited-field vs displayed-field check per column, `cellchange`-driven display repaint (never `editTemplate` handlers), definite height on the element, `row-number-offset`, pristine-row restore on cancel | Turns four separately-discovered defects into an up-front checklist; each cost a full user-visible iteration to find | Pending |
| 2026-08-26 | Hand-escaped dynamic SOSL threw a user-visible error on ordinary punctuation (`[`, `]`, `\|`) in a search term, because making a character literal for the SOSL engine and getting a backslash past the query parser are two different escaping layers | Activities List search box; see `cases/user-search-text-hand-escaped-sosl-vs-bound-soql.md` | Add to `apex` skill: never concatenate user-supplied text into dynamic SOQL/SOSL - pre-resolve related records to Id sets with bound SOQL and filter on those; if the search index is required, isolate escaping in one tested helper. Require search regression tests to assert a record with reserved characters is *returned*, not merely that no exception is thrown | Removes a class of error that only appears on realistic input, and that an assert-no-throw test passes trivially against an empty result | Pending |
| 2026-08-26 | Apex assertion reconstructed a date-only value at local midnight while production placed it at GMT midnight, making a sort test pass or fail depending on the time of day it ran | Activities List sort test; passed one evening, failed the next morning with no change to the class under test; see `cases/apex-test-local-vs-gmt-midnight-time-of-day-flakiness.md` | Add to `apex` skill test guidance: build temporal test data from fixed instants rather than `Date.today()`/`Datetime.now()`, and treat any `newInstance` vs `newInstanceGmt` choice in a test as something to check against the production path | Prevents time-of-day flakiness, which reads as unreliable infrastructure and erodes trust in every other assertion in the suite | Pending |
